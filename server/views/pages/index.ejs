<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/server/client/CSS/style.css">
    <title>Document</title>
</head>
<body>
    <header>
        <a href="#">logo</a>
        <a href="#">Om oss</a>
        <a href="#">Kontakt</a>
    </header>
    <main>
        <input name="input" type="text" placeholder="Sök här (ex. hur gör man en for-loop)">
        <% for( let index = 0; index < posts.length; index++ ) { %>
            <h1><%= posts[index].title %> </h1>
            <a href="./post?post=<%= posts[index].title %>">tjo <%= index %> </a>
        <% } %> 
    </main>
</body>
</html>